#ifndef CRYPTAGE_HPP
#define CRYPTAGE_HPP

#include <iostream>
#include <string>
#include <bitset>

using namespace std;

bitset<8> conversion(unsigned char byte)
{
    return bitset<8>(byte);
}

unsigned char crypter(unsigned char contenu, bitset<8> clef){
    
}

#endif
